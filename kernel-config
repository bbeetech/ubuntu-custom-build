# Android Binder support
CONFIG_ANDROID=y
CONFIG_ANDROID_BINDER_IPC=y
CONFIG_ANDROID_BINDERFS=y
CONFIG_ANDROID_BINDER_DEVICES="binder,hwbinder,vndbinder"

# Enable MACVLAN
CONFIG_MACVLAN=y

# Enable cfg80211 (802.11 Wi-Fi virtual interface support)
CONFIG_MAC80211=y